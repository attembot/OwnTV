# OwnTV TMDB caching proxy — Cloudflare Worker config.
# Deploy with:  npx wrangler deploy
# Set the secret once:  npx wrangler secret put TMDB_KEY
name = "owntv-tmdb-meta"
main = "index.js"
compatibility_date = "2026-01-01"
